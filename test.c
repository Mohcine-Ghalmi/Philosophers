#include "philosophers.h"

int main(void)
{
    // printf("befor usleep\n");
    // int us = usleep(3000000); // 3s
    // printf("%d || after usleep\n", us);
    // settimeofday()
    struct timeval *time = NULL;
    gettimeofday(time, NULL);
}
